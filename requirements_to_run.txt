# Save the content from the Python 3.12 Compatible Requirements artifact to a file named requirements.txt
# Deactivate any active virtual environment
deactivate
# Remove old virtual environment
Remove-Item -Path .venv -Recurse -Force -ErrorAction SilentlyContinue
# Create a new virtual environment
python -m venv .venv
# Activate the new environment
.\.venv\Scripts\Activate.ps1
# Update pip
python -m pip install --upgrade pip
# Install numpy first (critical dependency)
pip install numpy==1.26.4
# Install packages one by one in order of importance
pip install scipy==1.11.3
pip install pandas==2.1.1
pip install matplotlib==3.8.2
pip install scikit-learn==1.3.2
pip install imbalanced-learn==0.11.0

# Install deep learning libraries
pip install tensorflow==2.16.1
pip install keras==3.0.1
pip install torch==2.2.0

# Install other packages
pip install opencv-python
pip install opencv-contrib-python
pip install scikit-image
pip install timm==0.9.7

#run this command
# $env:PYTHONPATH = "D:\SCI PAPER\Updated_code\paper1"; python Main\GUI.py
$env:PYTHONPATH = "D:\SCOPUS PAPER\Updated_code\paper2"; python Main\GUI.py